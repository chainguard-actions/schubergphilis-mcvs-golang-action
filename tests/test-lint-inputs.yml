name: Test lint inputs (testing-type, token, build-tags, golangci-timeout)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        with:
          go-version-file: "tests/fixtures/go.mod"
          testing-type: "lint"
          token: ${{ github.token }}
          build-tags: "unit"
          golangci-timeout: "5m"
      - name: Assert action ran with lint inputs
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "success" ]; then
            echo "PASS: lint action succeeded"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed gracefully (lint requires project Taskfile/golangci config)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
